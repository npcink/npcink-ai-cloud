# 全仓文档语义盘点（2026-09-11）

状态：historical inventory；不是当前规范或发布授权。

## 目的与边界

本盘点为后续全仓文档整理建立事实基线。只检查 Markdown 文件的标题、前置状态、入口可达性和明显的权威措辞，不自动改写正文，不删除历史证据，也不把历史材料升级为当前规则。

## 当前基线

- `docs/` 共 435 份 Markdown（含本盘点）。
- 76 份文档在前 12 行没有标准 `Status`/`状态` 字段；这不是内容错误，但降低了读者判断当前性的效率。
- 99 份被识别为 active/current，99 份明确为 historical，161 份属于其他状态表达，后续需要人工按语义复核；统计是导航线索，不是自动裁决。
- 现有入口、ADR、工程标准和历史证据混合分布；README 是入口，但不能替代每份文档自身的状态声明。

## 整理规则

1. 当前规范必须明确 `Status: active`，并说明它拥有的决策范围。
2. 当前指南、计划、回顾和验收记录必须说明是否可作为现行操作依据。
3. 历史证据保留原文，补充“历史/不覆盖当前代码与规范”的边界说明，不删除或伪造替代链接。
4. 被多份文档重复声明的规则，以当前代码、测试、ADR 和 active standard 为准；其他文档只保留引用关系。
5. 每次修订限于一个语义簇（例如 M4、Admin、发布或结构整改），完成链接、状态和冲突检查后再进入下一簇。

## 下一批处理顺序

1. 先处理入口级文档：`docs/README.md`、README 直链和当前 active standards。
2. 再处理运行与发布：M4、生产、连接器和管理员操作文档。
3. 再处理计划、回顾和验收记录：统一历史状态和当前引用。
4. 最后处理无状态文档和 legacy 快照；逐份人工判断，不用行数或日期自动归档。

## 完成标准

每个语义簇应有：入口链接、状态声明、权威来源、过期内容处理说明、断链检查和变更清单。全仓语义整理只有在所有语义簇完成并经人工复核后才能声称完成；本文件只完成盘点，不声称全仓治理完成。

## 证据

本轮统计由只读脚本生成，临时明细保存在 ignored `.tmp-docs-semantic-inventory.json`，不作为仓库事实来源。后续批次应重新扫描当前工作区。
