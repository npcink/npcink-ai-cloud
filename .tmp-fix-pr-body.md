## Summary

Allow the read-only `certificate-readiness` maintenance action to validate a
first-install host before `current` exists, while keeping unsafe release paths
fail-closed.

## Scope

- [x] This change is limited to the production maintenance readiness module.
- [x] No unrelated generated files, local environment files, screenshots, or cross-repo worktree changes are included.
- Focused module: `.github/workflows/production-maintenance.yml` and its contract test.
- Explicit non-goals: no certificate issuance, no host mutation, no production dispatch, and no change to deploy bundle logic.
- Cloud boundary impact: none; this remains an operator-only read-only release diagnostic.

## Release Gate

- [x] This PR does not commit production secrets, SMTP passwords, provider keys, DB credentials, or internal tokens.
- [x] This PR does not make Cloud a WordPress write owner, second ability registry, second workflow registry, or second local control plane.

## Cloud Boundary

- [x] Cloud remains the hosted runtime enhancement layer.
- [x] This does not add a second WordPress control plane, local ability registry, workflow registry, approval/preflight/audit truth, or WordPress write owner.
- [x] Provider credentials, request logs, usage, and entitlement evidence remain redacted from logs and responses.

## Verification

- [x] Focused contract pytest passed: `tests/contract/test_production_secret_argv_contract.py -k production_workflows_serialize_host_mutation_and_confirm_prune`.
- [x] Ruff passed for the changed contract test.
- [x] `git diff --check` passed.

## Risk

- Residual risk: the remote maintenance action remains dependent on the governed readiness receipt and root-owned SSH environment.
- Rollback plan: revert this two-file commit; no production host mutation is part of the change.

## Deployment Impact

- [x] No production deployment impact.

## Notes

The maintenance action now mirrors deploy preflight semantics: absent `current`
is accepted for first install; non-symlink, broken, and out-of-root paths fail.
